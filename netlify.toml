[build]
  command   = "pnpm --filter @workspace/chumani-landscapes run build"
  publish   = "artifacts/chumani-landscapes/dist/public"

[build.environment]
  BASE_PATH    = "/"
  PORT         = "3000"
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
